name: norovirus
channels:
  - conda-forge
  - bioconda
 
dependencies:
  - bygul
  # Runtime
  - python=3.11.4
 
  # Reference/medoid selection and competitive mapping
  - barcodeforge=1.1.5
  - bwa-mem2=2.3
  - samtools
  - seqkit
 
  # File transfer and decompression
  - wget
  - curl
  - zstd
 
  # Python analysis libraries used by the scripts
  - pandas
  - biopython
  - tqdm
  # File transfer and compression
  - wget
  - curl
  - zstd
  - pigz

  # Python analysis libraries used by the scripts
  - pandas
  - numpy
  - scipy
  - biopython
  - tqdm
  - pysam
  - pip:
      - pandas
      - biopython
      - tqdm
